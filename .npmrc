# Évite le téléchargement Chrome (~150 Mo) à chaque npm install.
# En dev : PDF_RENDERER=mock (défaut dans .env.example).
# En prod/Docker : Chromium système via PUPPETEER_EXECUTABLE_PATH.
puppeteer_skip_download=true
